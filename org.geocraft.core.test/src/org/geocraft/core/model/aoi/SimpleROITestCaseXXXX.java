/*
 * Copyright (C) ConocoPhillips 2008 All Rights Reserved. 
 */
package org.geocraft.core.model.aoi;


import junit.framework.TestCase;


/**
 * Unit tests for the <code>SimpleROI</code> class.
 */
public class SimpleROITestCaseXXXX extends TestCase {

  /**
   * Unit test for ROI with a z range.
   */
  public void testROIwithZRange() {
    //    // Create an AOI.
    //    MapPolygonAOI aoi = new MapPolygonAOI("aoi");
    //
    //    // Add an inclusive polygonal area.
    //    double[] x = new double[] { 0, 10, 10, 0, 0 };
    //    double[] y = new double[] { 0, 0, 10, 10, 0 };
    //    aoi.addInclusionPolygon(x, y);
    //
    //    // Create a ROI from the AOI, with a z range.
    //    Unit zUnit = Unit.METER;
    //    SimpleROI roi = new SimpleROI("roi", aoi, 1000, 2000, ZDomain.DEPTH);
    //
    //    // Validate certain x,y,z coordinates are inside or outside the ROI.
    //    assertTrue(roi.contains(5, 5, 1400, zUnit));
    //    assertFalse(roi.contains(50, 50, 1600, zUnit));
    //    assertTrue(roi.contains(0, 0, 2000, zUnit));
    //    assertFalse(roi.contains(0, 0, 800, zUnit));
    //    assertTrue(roi.contains(0, 5, 1000, zUnit));
    //    assertFalse(roi.contains(0, 5, 2100, zUnit));
    //    assertTrue(roi.contains(5, 0, 1500, zUnit));
    //    assertFalse(roi.contains(5, 0, 4000, zUnit));
    //    assertFalse(roi.contains(10, 5, 1500, zUnit));
    //    assertFalse(roi.contains(5, 10, 1500, zUnit));
  }
  /**
   * Unit test for ROI without a z range.
   */
  //  public roid testROIwithoutZRange() {
  //    // Create an AOI.
  //    MapPolygonAOI aoi = new MapPolygonAOI("aoi");
  //
  //    // Add an inclusive polygonal area.
  //    double[] x = new double[] { 0, 10, 10, 0, 0 };
  //    double[] y = new double[] { 0, 0, 10, 10, 0 };
  //    aoi.addInclusionPolygon(x, y);
  //
  //    // Create a ROI from the AOI, without a z range.
  //    Unit zUnit = Unit.FOOT;
  //    SimpleROI roi = new SimpleROI("roi", aoi);
  //
  //    // Validate certain x,y,z coordinates are inside or outside the ROI.
  //    assertTrue(roi.contains(5, 5, 1400, zUnit));
  //    assertFalse(roi.contains(50, 50, 1600, zUnit));
  //    assertTrue(roi.contains(0, 0, 2000, zUnit));
  //    assertTrue(roi.contains(0, 0, 800, zUnit));
  //    assertTrue(roi.contains(0, 5, 1000, zUnit));
  //    assertTrue(roi.contains(0, 5, 2100, zUnit));
  //    assertTrue(roi.contains(5, 0, 1500, zUnit));
  //    assertTrue(roi.contains(5, 0, 4000, zUnit));
  //    assertFalse(roi.contains(10, 5, 1500, zUnit));
  //    assertFalse(roi.contains(5, 10, 1500, zUnit));
  //  }

}
